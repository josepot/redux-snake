export const ROWS = 20;
export const COLS = 30;
export const MARGIN = { LEFT: 1, RIGHT: 1, TOP: 1, BOTTOM: 4 };
export const GROWTH_FACTOR = 4;
export const TICK_FREQUENCY = 100;
export const MAXIMUM_INITIAL_OFFSET = 1000;

export const initialHead = {
  x: Math.trunc(COLS / 2),
  y: Math.trunc(ROWS / 2),
};
